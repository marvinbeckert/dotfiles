- clean: ['~']

- link:
    ~/.gitconfig:
    ~/.ssh/config:
        path: ssh/config
        create: true
    ~/.vimrc:
    ~/.zshrc:
    ~/.shell:
        create: true
    ~/.tmux.conf:
    ~/.p10k.zsh:
