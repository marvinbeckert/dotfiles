- clean: ['~']

- link:
    ~/.gitconfig:
    ~/.ssh/config:
        path: ssh/config
        create: true
    ~/.vimrc:
    ~/.zshrc:
    ~/.shell:
        create: true
    ~/.tmux.conf:
    ~/.oh-my-zsh/:
        relink: true
    ~/.p10.zsh:
