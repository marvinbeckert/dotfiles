- clean: ['~']

- link:
    ~/.gitconfig:
    ~/.ssh/config:
        path: ssh/config
        create: true
    ~/.vimrc:
    ~/.zshrc:
    ~/.shell/aliases:
        path: ./shell/aliases.sh
        create: true
    ~/.tmux.conf:
